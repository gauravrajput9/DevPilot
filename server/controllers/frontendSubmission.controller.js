/**
 * Frontend Submission Controller (Stub)
 */

export const runFrontendCode = async (req, res) => {
  return res.status(501).json({
    success: false,
    message: "Frontend code execution is not yet implemented",
  });
};

export const submitFrontendCode = async (req, res) => {
  return res.status(501).json({
    success: false,
    message: "Frontend code submission is not yet implemented",
  });
};

